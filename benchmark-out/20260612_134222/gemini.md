# `pythonCookbookData.ts` Module Documentation

## Purpose

The `pythonCookbookData.ts` module acts as a structured data store within the VS Code Copilot extension. Its primary purpose is to provide "cookbook" examples and explanations for various Python code improvement rules, likely derived from linting tools such as Ruff. These data entries are used to generate contextual prompts or suggestions, helping users understand and apply best practices to their Python code. The module is explicitly marked as auto-generated, indicating it is compiled from another source and should not be edited manually.

## Key Components and Responsibilities

-   **`PromptMap` Type:**
    ```typescript
    type PromptMap = Record<string, string>;
    ```
    This type alias defines the structure for the cookbook data. It's a simple key-value pair map where keys are string identifiers (e.g., linting rule codes) and values are strings containing the detailed explanation and code examples.

-   **`pythonRuffCookbooks` Constant:**
    ```typescript
    export const pythonRuffCookbooks: PromptMap = {
        'AIR001': '...',
        'AIR301': '...',
        'ERA001': '...',
        'FAST001': '...',
        'YTT101': '...',
        'ANN001': '...',
        // ... more entries
    };
    ```
    This is the main export of the module. It's an object conforming to the `PromptMap` type, containing a collection of Python-related code examples and their descriptions. Each key (e.g., `AIR001`, `FAST001`, `YTT101`, `ANN001`) corresponds to a specific rule or pattern, often referencing common Python libraries (like Airflow, FastAPI) or general coding practices (like type annotations). The associated string value provides a clear explanation, typically including "Before" and "After" code snippets to illustrate the suggested change.

## Important Patterns and Design Decisions

1.  **Auto-Generation:** The module includes a prominent comment: `// this file is automatically generated. Do not edit it.` This highlights a critical design decision: the content is maintained and generated by an automated process. This ensures consistency, reduces manual errors, and allows for scalable updates to the cookbook data without direct developer intervention in this file. It implies a build-time process that collects Python linting rules or code patterns from an external source and transforms them into this TypeScript `PromptMap`.

2.  **Rule-Based Indexing:** The use of short, alphanumeric codes (e.g., `AIR001`, `FAST001`, `YTT101`, `ANN001`) as keys for the `PromptMap` suggests a systematic way of referencing specific coding rules or recommendations. This allows for quick lookup and association of prompts with detected code issues or user queries. The prefixes (e.g., `AIR`, `FAST`, `YTT`, `ANN`) likely denote categories or origins of these rules (e.g., Airflow-related, FastAPI-related, specific Ruff checks for `YTooManyTupleLiterals` or `AnnAssign`).

3.  **Descriptive Prompt Content:** Each value in the `PromptMap` is a comprehensive string designed to be directly consumable by an AI or presented to a user. It includes:
    *   A concise summary of the rule.
    *   A clear rationale for the change.
    *   "Before" code examples demonstrating the problematic pattern.
    *   "After" code examples showing the corrected or improved pattern.
    This format is ideal for teaching and guiding users through code refactoring.

## How this Module Fits into the Larger Project

Given its location (`extensions/copilot/src/extension/prompts/node/inline/pythonCookbookData.ts`) within the VS Code repository, this module is an integral part of the **Copilot extension**.

-   **Contextual Code Assistance:** This module likely provides the underlying data for Copilot's intelligent code suggestions, explanations, and automated fixes for Python. When a user writes Python code that triggers a known linting rule (e.g., `AIR001`), Copilot can reference `pythonRuffCookbooks` to retrieve the relevant explanation and suggested fix, presenting it inline or as part of its assistance.
-   **Node.js Environment:** The `node` segment in the path indicates that this module is consumed within a Node.js environment, which is typical for VS Code extensions.
-   **Inline Suggestions:** The `inline` segment suggests that the data from this module is primarily used for providing quick, on-the-fly suggestions or explanations directly within the editor interface, rather than requiring the user to navigate to external documentation.
-   **Extensibility and Maintainability:** By having this data in a separate, auto-generated module, the Copilot team can easily update, expand, or modify the Python cookbook content without altering the core logic of the extension itself. This separation of concerns improves the maintainability and scalability of the code assistance features.